<footer>
  <div class='container'>
    <p class='text-muted'>
      <% if request.path == root_path %>
        <a class='invite' href='<%= new_user_registration_path %>'>Присоединиться</a>
      <% else %>
        PaySquad.ru
      <% end %>
    </p>
  </div>
</footer>
